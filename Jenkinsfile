pipeline {
    agent: any

    stages {
        stage('Checkout') {
            steps {
                sh '''
                    echo "Hello from Jenkins Pipeine"
                '''
            }
        }
    }
}